import java.util.*

fun main() {
//    val scanner = Scanner(System.`in`)
//    val size = scanner.nextInt()
//    val elements = IntArray(size) { scanner.nextInt() }
//    val rightShifts = scanner.nextInt()
//
//    val shiftedArray = IntArray(size)
//    for (i in elements.indices) {
//        shiftedArray[(i + rightShifts) % size] = elements[i]
//    }
//    for (num in shiftedArray) {
//        print("$num ")
//    }
    var i = 3
    while (i > 3) do {
        println(i)
        i--
    }
    println("Completed")
}